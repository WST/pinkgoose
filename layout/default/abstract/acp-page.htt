<!DOCTYPE html>
<html>
	<head>
		<title>Администрирование — {{ title }}</title>
	</head>
	<body>
		<h1>{{ title }}</h1>
		{% block body %}
			<div>{{ content }}</div>
		{% endblock %}
	</body>
</html>
