{% extends 'abstract/page.htt' %}

{% block article %}
	{% for post in posts %}
		<h2>{{ post.title }}</h2>
		<div>{{ post.intro }}</div>
	{% else %}
		<h2>Нет ни одного поста</h2>
	{% endfor %}
{% endblock %}
